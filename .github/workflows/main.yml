name: My-Daily-AI-Task
on: [push] # 只要你改了代码，它就自动运行

jobs:
  run-my-ai:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run the Agent
        run: python my_agent.py
